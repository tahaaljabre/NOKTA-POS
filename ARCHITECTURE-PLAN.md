# خطة إعادة هيكلة NOKTA POS

آخر تحديث: 2026-09-09

هذه الوثيقة هي الخطة المعتمدة لإعادة هيكلة المشروع تدريجيًا من دون تغيير السلوك التجاري أو كسر التوافق مع الأجهزة الحالية. لا يبدأ نقل الملفات قبل إصلاح خط أساس الاختبارات وتسجيل نتيجة كل مرحلة في `audit/CHANGELOG.md`.

## 1. نتيجة دراسة الوضع الحالي

المشروع يعمل كتطبيق Express وSQLite وواجهة Vanilla JavaScript وPWA، وله اختبارات تغطي سلامة الطلبات والمعاملات والمزامنة والصلاحيات. نقاط القوة التي يجب الحفاظ عليها:

- كل إنشاء أو تعديل أو حذف للطلبات يمر عبر `src/services/orders.service.js`.
- الحسابات والآثار الجانبية للطلب تحفظ داخل معاملة واحدة.
- قاعدة الاختبارات في الذاكرة، مع حماية قاعدة المنشأة من الاختبارات.
- الواجهة عربية/إنجليزية، والعمل دون اتصال لا يخزن استجابات API.

أكبر مصادر التعقيد الحالية:

- `public/style.css`: قرابة 3000 سطر ويجمع تنسيق كل الشاشات.
- `public/js/orders.js`: أكثر من 1100 سطر ويجمع السلة والدفع والطباعة والعملاء والطلبات النشطة.
- `public/js/daily-closing.js`: قرابة 1000 سطر ويجمع الوردية والإغلاق والطباعة.
- `public/index.html`: قرابة 970 سطر ويجمع كل النوافذ والنماذج.
- `public/js/app.js`: حالة عامة وAPI ودخول وتنقل ومزامنة وSocket في ملف واحد.
- الاعتماد على متغيرات ودوال عامة بين ملفات الواجهة يجعل القص المباشر للملفات خطرًا.
- `PROJECT-SPEC.md` تاريخي وفيه معلومات تجاوزها التنفيذ؛ الكود و`PROJECT-HANDOFF.md` هما المرجع التشغيلي.
- مشغل `pnpm test` الحالي قد يسجل الاختبار `FAIL` بعد مهلة 30 ثانية رغم أن الاختبار نفسه يطبع `PASS`؛ يجب إصلاح إغلاق الموارد أو آلية التشغيل قبل اعتبار الاختبارات بوابة آمنة للتقسيم.

حالة التنفيذ في 2026-09-09: اكتملت مراحل تقسيم الواجهة 0–4، وبدأت المرحلة 6 بوحدة شؤون الموظفين والحضور. أضيفت وحدة HR مستقلة بجداول ومسارات وصلاحية `attendance` وترجمة كاملة. الاختبارات الحالية 27/27 والفحص العام يشمل 120 ملف JavaScript والأصول. المرحلة 7 (Adapter جهاز محدد) تبدأ فقط بعد اعتماد موديل البصمة ومواصفاته.

## 2. المبادئ المعمارية الملزمة

1. التقسيم حسب الميزة التجارية، لا حسب نوع الملف فقط.
2. لكل ميزة واجهة عامة صغيرة؛ لا يُسمح بالوصول العشوائي إلى حالتها الداخلية.
3. المسار HTTP يستقبل ويتحقق من الشكل الأولي فقط، والخدمة تنفذ قواعد العمل، والمستودع يتعامل مع SQL.
4. لا منطق تسعير أو مخزون أو نقاط خارج مجال الطلبات وخدماته المعتمدة.
5. لا نص ظاهر بلا مفتاح ترجمة عربي وإنجليزي واختبار تطابق للمفاتيح.
6. لا نافذة كبيرة داخل `index.html` بعد نقلها؛ لكل شاشة وحدة عرض ووحدة تحكم وقالب قابل للاختبار.
7. كل مرحلة ترحيل تحافظ على API والسلوك الحاليين، وتملك اختبارات قبل/بعد.
8. أجهزة البصمة تكامل خارجي عبر موصلات؛ لا تعتمد النواة على شركة أو جهاز بعينه.
9. لا تُخزّن صور البصمات في NOKTA POS. يخزن النظام معرف القالب الخارجي أو معرف المستخدم في الجهاز فقط، مع سجل موافقة وتدقيق.

## 3. الهيكل المستهدف

```text
src/
  core/
    config/ database/ errors/ http/ security/ audit/ events/
  modules/
    auth/
    employees/
    attendance/
    orders/
    catalog/
    inventory/
    customers/
    tables/
    shifts/
    reports/
    settings/
    printing/
    sync/
  integrations/
    biometrics/
      contracts/ adapters/ discovery/ jobs/
    printers/
  app.js

public/
  app/
    core/
      api/ auth/ state/ events/ router/ i18n/ offline/ realtime/ ui/
    features/
      pos/ orders/ employees/ attendance/ catalog/ inventory/
      customers/ tables/ shifts/ reports/ settings/ audit/ kds/
    shared/
      components/ formatters/ validators/ printing/
    bootstrap.js
  styles/
    tokens.css
    base.css
    layout.css
    components/
    features/
  index.html
```

داخل كل وحدة خلفية يفضل الشكل التالي عند الحاجة فقط:

```text
employees/
  employees.routes.js
  employees.controller.js
  employees.service.js
  employees.repository.js
  employees.validation.js
  employees.permissions.js
```

لا تنشأ طبقات فارغة لمجرد مطابقة الهيكل. يبدأ الملف الواحد ثم يفصل عندما يملك سببًا واضحًا للاختبار أو التغيير.

## 4. تقسيم الواجهة الكبيرة

### قاعدة تسمية الملفات

- الاسم إنجليزي كامل بصيغة kebab-case ويبدأ باسم المجال: `order-payment.js` وليس `payment.js`.
- الملف الذي يعرض قائمة يسمى `*-list.js`، والمحرر `*-editor.js`، والنموذج `*-form.js`، والطباعة `*-printing.js`.
- لا تستخدم أسماء مثل `utils2.js` أو `common-new.js` أو `temp.js`.
- مجلد الميزة بصيغة الجمع، والملف بصيغة المفرد: `features/orders/order-payment.js`.
- الاختبار يحمل نفس اسم الوحدة أو السلوك: `order-payment.test.cjs` عند الانتقال لاحقًا إلى اختبارات الوحدات.

خريطة الأسماء المعتمدة للملفات الكبيرة:

```text
features/orders/
  order-cart.js
  order-modifiers.js
  order-save.js
  order-active-list.js
  order-payment.js
  order-customer-picker.js
  order-receipt-printing.js
  order-production-printing.js

features/shifts/
  shift-opening.js
  shift-employee-closing.js
  shift-business-day-closing.js
  shift-closing-report.js
  shift-closing-printing.js

features/catalog/
  catalog-categories.js
  catalog-items.js
  catalog-modifiers.js
  catalog-search.js

features/settings/
  settings-general.js
  settings-printers.js
  settings-payment-methods.js
  settings-delivery-methods.js
  settings-backup.js
  settings-network.js
```

الحالة المنفذة: نُقلت طباعة الإنتاج إلى `order-production-printing.js` وطباعة إيصال العميل إلى `order-receipt-printing.js` وقائمة الطلبات النشطة إلى `order-active-list.js`. لا يعاد دمج وحدتي الطباعة لأن لكل منهما مستهلكًا وإعدادات ومخرجًا مختلفًا، ولا تدمج القائمة مع الدفع لأن عرض الطلبات ودورة تسديد الطلب مسؤوليتان منفصلتان.

### النواة

- `core/api`: عميل HTTP موحد، معالجة الأخطاء، وإلغاء الطلبات.
- `core/state`: مخزن حالة صغير بواجهات `getState/dispatch/subscribe` بدل المتغيرات العامة.
- `core/events`: أسماء أحداث ثابتة وحمولة موثقة.
- `core/router`: إظهار الشاشة، الحراسة بالصلاحية، ودورة حياة `mount/unmount`.
- `core/i18n`: `t` والاتجاه والتنسيق واختبار المفاتيح.
- `core/offline`: IndexedDB وطابور المزامنة فقط.
- `core/realtime`: Socket.IO وتحويل رسائله إلى أحداث داخلية.

### شاشة البيع والطلبات

يفصل `orders.js` إلى وحدات: `cart`, `pricing-preview`, `modifiers`, `order-editor`, `payment`, `active-orders`, `receipt`, `production-tickets`, و`customers-picker`. الحساب في المتصفح للعرض فقط، والنتيجة المعتمدة دائمًا من الخادم.

### الورديات والإغلاق

يفصل `daily-closing.js` إلى: `shift-opening`, `employee-closing`, `business-day-closing`, `closing-report`, و`closing-print`. يمنع تكرار استعلامات الطلبات والحسابات بين مسارات الطباعة والعرض.

### HTML وCSS

- تبقى في `index.html` قشرة التطبيق وعناصر الجذر فقط.
- تنشأ الشاشات من قوالب آمنة لا تضع بيانات غير موثوقة في HTML أو `onclick`.
- يقسم CSS إلى رموز تصميم وأساس وتخطيط ومكونات وملفات لكل ميزة.
- تعتمد أسماء CSS بنطاق الميزة مثل `pos-`, `attendance-`, `employee-` لتقليل التصادم.

## 5. نظام الموظفين والحضور المستقبلي

يفصل مفهوم حساب الدخول عن ملف الموظف وعن عقد العمل:

- `employees`: الهوية والاسم والحالة والفرع.
- `user_accounts`: بيانات الدخول والدور وإبطال الجلسات.
- `employment_contracts`: تاريخ البداية، نوع الأجر، الساعات القياسية، وسياسة الإضافي.
- `work_schedules`: جدول الأيام والأوقات وفترات الراحة.
- `attendance_events`: دخول/خروج خام مع المصدر والجهاز والوقت والمنطقة الزمنية.
- `attendance_days`: النتيجة اليومية المحتسبة والقابلة للمراجعة.
- `attendance_adjustments`: تعديل يدوي بسبب ومرجع تدقيق.
- `overtime_rules`: قواعد الإضافي والعطل ونقص الساعات.
- `leave_requests`: الإجازات والغياب والموافقات.
- `devices`: سجل أجهزة الحضور وحالتها وآخر مزامنة.
- `employee_device_identities`: ربط الموظف بمعرفه في كل جهاز.

قواعد مهمة:

- يسجل الحدث الخام كما ورد ولا يعدل؛ التصحيح يضاف كسجل جديد.
- يحسب يوم العمل بواسطة `business-time.js` والمنطقة الزمنية للمنشأة.
- يفصل الحضور عن الرواتب؛ الحضور ينتج ساعات معتمدة، والرواتب تستهلكها لاحقًا.
- كل اعتماد أو تعديل يدوي يحتاج صلاحية مستقلة وسجل تدقيق قبل/بعد.
- منع التكرار بمفتاح خارجي ثابت يتكون من الجهاز ومعرف الحدث.

## 6. طبقة أجهزة البصمة

لا يوجد موصل واحد يضمن التعرف على كل أجهزة البصمة؛ الشركات تختلف في SDK والبروتوكول وطريقة تخزين القوالب. الحل القابل للتوسع هو عقد موحد:

```text
BiometricAdapter
  discover()
  connect(config)
  healthCheck()
  listUsers(cursor)
  pullEvents(sinceCursor)
  mapEmployee(deviceUserId, employeeId)
  pushEmployee(employee)
```

التنفيذ المقترح:

- خدمة ربط محلية على كمبيوتر المطعم تتواصل مع الأجهزة داخل LAN.
- Adapter مستقل لكل عائلة أجهزة: API شبكي، SDK خاص بـWindows، ملف تصدير، أو Webhook.
- جدول قدرات لكل Adapter: قراءة أحداث، إضافة مستخدم، حذف مستخدم، مزامنة فورية، أو مزامنة دورية.
- طابور مزامنة قابل لإعادة المحاولة، ومؤشر `cursor` لكل جهاز، وسجل أخطاء لا يوقف بقية الأجهزة.
- لا يوعد بدعم جهاز قبل معرفة الشركة والموديل والبروتوكول والحصول على SDK أو توثيق رسمي.

## 7. مراحل التنفيذ الآمنة

### المرحلة 0 — خط الأساس والحوكمة

- إصلاح خروج الاختبارات وتنظيم نتيجة `pnpm test` بحيث لا تسجل نجاحًا كفشل زائف.
- إضافة اختبارات دخان لكل شاشة وصلاحية وترجمة.
- اعتماد `audit/CHANGELOG.md` وبروتوكول التغيير.
- توثيق API الحالية وقواعد البيانات قبل النقل.

معيار الخروج: `pnpm test` و`pnpm run check` ينجحان بوضوح مرتين متتاليتين، من دون لمس `data/pos.sqlite`.

### المرحلة 1 — عقود الواجهة من دون تغيير الشاشات

- إضافة عميل API ومخزن حالة وناقل أحداث وRouter.
- وضع أغلفة توافق حول الدوال العامة الحالية.
- اختبارات للعقود والأحداث والمزامنة.

معيار الخروج: تعمل النسخة القديمة عبر الأغلفة الجديدة ولا يتغير HTML المرئي.

### المرحلة 2 — تقسيم شاشة البيع والطلبات

- نقل الوحدات واحدة في كل إصلاح مستقل.
- البدء بالإيصال والطباعة، ثم العملاء، ثم الدفع، ثم محرر الطلب، ثم السلة.
- مقارنة استجابات API والإيصالات قبل/بعد.

### المرحلة 3 — تقسيم الوردية والإغلاق

- توحيد تحميل بيانات الإغلاق والحسابات.
- فصل العرض والطباعة والإجراءات.
- إضافة حالات المنطقة الزمنية، منتصف الليل، والوردية الممتدة.

### المرحلة 4 — تقسيم بقية الشاشات وCSS/HTML

- المنيو، الإعدادات، التقارير، الموظفون، المخزون، الجداول، والتدقيق.
- نقل شاشة واحدة في كل دفعة مع اختبار صلاحيات وترجمة ودخان.

### المرحلة 5 — وحدات الخادم

- نقل SQL من المسارات إلى repositories تدريجيًا.
- إبقاء URLs واستجابات API متوافقة.
- تقسيم `orders.service.js` داخليًا فقط مع واجهة عامة واحدة للكتابة.

### المرحلة 6 — الموظفون والحضور اليدوي

- migrations إضافية فقط، من دون إعادة كتابة تاريخ قديم.
- الجداول والسياسات والحضور اليدوي والتقارير والموافقات.
- اختبار الإضافي والنقص والعطلات والوردية العابرة لمنتصف الليل.

### المرحلة 7 — أول جهاز بصمة

- اختيار جهاز تجريبي محدد وبناء Adapter له.
- تشغيل وضع قراءة فقط أولًا ومقارنة السجلات يدويًا.
- بعد الاعتماد، تفعيل الربط ثنائي الاتجاه إذا كان الجهاز يدعمه.

### المرحلة 8 — التوسع

- إضافة Adapters أخرى من دون تغيير مجال الحضور.
- تهيئة تعدد الفروع والمحطات إذا أصبح مطلوبًا.

## 8. تعريف الإنجاز لكل دفعة

لا تعتبر الدفعة منتهية حتى يتحقق الآتي:

- لا تتغير قواعد الأسعار أو الضرائب أو المخزون أو النقاط بلا متطلب صريح.
- توجد migration متوافقة لكل تغيير قاعدة بيانات.
- النصوص الظاهرة موجودة بالعربية والإنجليزية.
- صلاحيات الخادم مختبرة، وليس مجرد إخفاء زر.
- اختبار الوحدة/التكامل الخاص بالتغيير ناجح، ثم `pnpm test` و`pnpm run check`.
- تحديث إصدار أصول PWA عند تغيير الواجهة.
- تحديث `audit/CHANGELOG.md` و`PROJECT-HANDOFF.md` عند نقطة تسليم كبيرة.
- تسجيل الملفات المتغيرة، قرار التصميم، الاختبارات، المخاطر، والخطوة التالية.

## 9. ممنوعات أثناء إعادة الهيكلة

- إعادة كتابة الواجهة كلها دفعة واحدة أو إدخال إطار Frontend أثناء مرحلة التقسيم.
- تغيير API وقاعدة البيانات والواجهة في دفعة واحدة بلا طبقة توافق.
- نسخ منطق الطلبات إلى sync أو الواجهة.
- دمج الحضور والرواتب والبصمة في وحدة واحدة.
- تخزين قالب بصمة أو صورة بصمة داخل قاعدة POS.
- حذف دالة قديمة قبل وجود اختبار يثبت انتقال جميع مستهلكيها.
- تشغيل اختبارات آلية على قاعدة المنشأة.
