# تعليمات العمل على NOKTA POS

اقرأ `PROJECT-HANDOFF.md` و`audit/REPAIR-LOG.md` و`ARCHITECTURE-PLAN.md` و`audit/CHANGE-PROTOCOL.md` وآخر إدخال في `audit/CHANGELOG.md` قبل أي تعديل. شغّل `pnpm test` بعد كل إصلاح مستقل، ثم `pnpm run check` بعد اكتمال المجموعة. الاختبارات تستخدم قاعدة `:memory:`؛ لا تشغّل اختبارات آلية على `data/pos.sqlite`.

كل مبرمج أو مساعد ذكاء اصطناعي ملزم بإضافة إدخال إلى `audit/CHANGELOG.md` قبل إنهاء أي دفعة، ويجب أن يسجل الهدف والملفات والقرار المعماري وقاعدة البيانات والترجمة والصلاحيات والاختبارات الفعلية والمخاطر ونقطة البدء التالية. لا يجوز وصف اختبار بأنه ناجح إذا لم يُشغّل أو إذا انتهى بمهلة أو خطأ.

## ثوابت لا يجوز كسرها

- Node.js 22.5 أو أحدث مطلوب لأن قاعدة البيانات تستعمل `node:sqlite` المدمج. مدير الحزم المعتمد هو pnpm وملف القفل هو `pnpm-lock.yaml`.
- كل كتابة لطلب تمر عبر `src/services/orders.service.js`؛ لا تنشئ مسار حساب أو حفظ مستقل للمزامنة أو الواجهة.
- الخادم يعيد حساب السعر والضريبة والخصم والإضافات. لا تثق في إجمالي أو سعر مرسل من المتصفح.
- رأس الطلب وعناصره ورقم الفاتورة والطاولة والمخزون والنقاط والتدقيق تحفظ داخل معاملة واحدة.
- `offline_id` ثابت للطلب المعاد إرساله، ولا يحذف العميل الطلب من الطابور إلا إذا أعاد الخادم معرّفه ضمن `syncedIds`.
- لا تخزن استجابات `/api` في Service Worker. العمل دون اتصال لا يتجاوز تسجيل الدخول.
- كل مسار حساس يحتاج `authenticate` و`requirePermission` المناسبين. النسخ الاحتياطي الكامل للمدير فقط.
- اعرض بيانات المستخدم باستخدام `escapeHtml` أو `textContent`، بما فيها قوالب الطباعة وخصائص HTML. لا تضع نصًا غير موثوق داخل `onclick`.
- عند إضافة نص ظاهر، أضف المفتاح بالعربية والإنجليزية في `public/i18n.js` وشغّل اختبار الترجمة.
- يوم العمل يحسب عبر `src/services/business-time.js` والمنطقة المخزنة في `business_timezone`، افتراضيًا `Asia/Bangkok`.
- الإنتاج يرفض البدء من دون `POS_TLS_KEY` و`POS_TLS_CERT`. سر الجلسات الدائم في مجلد البيانات ولا يدخل النسخ الاحتياطية.

## أوامر التحقق

```powershell
$env:CI='true'; pnpm install --frozen-lockfile
pnpm test
pnpm run check
pnpm run android:sync
```

اختبار واجهة آمن: `node scripts/demo-test.cjs` يشغل قاعدة مؤقتة على المنفذ 3100. الحساب: `testmanager` وكلمة المرور `Test!123`. لا تستخدم هذا الحساب في الإنتاج.

## مخاطر تشغيلية باقية

- يجب أن يزوّد المشغل شهادة TLS موثوقة للإنتاج؛ شهادات `audit/test-tls` للاختبار فقط.
- أرصدة المخزون والنقاط التي ربما تأثرت بأخطاء نسخة قديمة تحتاج تسوية تجارية يراجعها صاحب المنشأة؛ لا تعدّل التاريخ آليًا.
- النسخة الاحتياطية الكاملة تحتوي تجزئات بيانات الدخول، لذلك تحفظ مشفرة وخارج أجهزة الكاشير.
- دخول المدير البعيد مهيأ عبر Tailscale Serve. اقرأ `REMOTE-ACCESS.md`، ولا تفتح المنفذ 3000 للعامة.
