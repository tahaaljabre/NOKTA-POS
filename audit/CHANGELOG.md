# سجل تغييرات NOKTA POS

هذا السجل إلزامي لكل تغيير بشري أو آلي. أضف أحدث إدخال في الأعلى، ولا تعدل أو تحذف الإدخالات التاريخية إلا لتصحيح خطأ توثيقي مع ذكر سبب التصحيح.
## 2026-09-09 — إصلاح خطأ `taxRate is not defined` في شاشة الدفع

- المنفذ: Antigravity.
- الهدف: إصلاح الخلل البرمجي الذي يمنع المستخدم من الدفع بسبب عدم تعريف متغير نسبة الضريبة في نطاق تأكيد الدفع.
- النطاق: `public/js/features/orders/order-payment.js`.
- القرار: تعديل السطر الذي يمرر نسبة الضريبة للخادم (tax_percent) ليحسب المتغير بشكل سليم من إعدادات الفاتورة أو إعدادات النظام الحالية دون الاعتماد على متغير غير معرف في ذلك النطاق.
- قاعدة البيانات: لا يوجد.
- الاختبارات: جاري تشغيل الاختبارات.
- المخاطر: لا يوجد.
- نقطة البدء التالية: الإبلاغ بإصلاح الخلل.

## 2026-09-09 — إصلاح مشكلات الصلاحيات وشاشات الكاشير والمطبخ والطلبات النشطة

- المنفذ: Antigravity.
- الهدف: إصلاح عدم استجابة التطبيق لتطبيق الخصم عند تحديده للموظف، إصلاح ظهور شاشة الكاشير لمستخدمي المطبخ، وتعديل حالة الطلبات النشطة لتظهر "دفع" بدلاً من "تم" إذا كان الطلب غير مدفوع والعكس.
- النطاق:
  - `src/services/orders.service.js` و `public/js/core/app-state.js` لحل مشكلة تطبيق الخصم بناءً على `max_discount` و `discount_orders`.
  - `public/js/employees.js` لتحديد الصلاحيات تلقائياً بناءً على الدور المختار لتجنب الأخطاء، وإضافة توجيه تلقائي في `authentication.js`.
  - `public/js/features/orders/order-active-list.js` لتصحيح منطق ظهور أزرار (دفع / تم) اعتماداً على مقارنة مبلغ الدفع مع المجموع.
- القرار: 
  - تم تعديل `hasDiscountPerm` ليتحقق من الحد الأقصى للخصم بحيث إذا كان أقل من 100 وأكبر من 0 فإنه يسمح بالخصم للموظف كصلاحية صريحة.
  - تم تحديث حالة ظهور الزر "تم" في قسم الطلبات النشطة في حالة دفع كامل المبلغ.
- قاعدة البيانات: لا يوجد.
- الاختبارات: تم بنجاح تشغيل `pnpm test` (27/27) و `pnpm run check` واجتياز جميع الملفات.
- المخاطر: تم رفع التعديلات إلى المستودع الرئيسي بأمان، لا يوجد مخاطر.
- نقطة البدء التالية: تقديم تقرير للمستخدم بالمخرجات والتعديلات في هيكلة المشروع.

## 2026-09-09 — إضافة ميزة الدفع المسبق (Pre-paid) وإنهاء الطلب تلقائياً

- المنفذ: Antigravity.
- الهدف: إضافة خيار تحديد الطلب كـ "مدفوع مسبقاً" من واجهة الكاشير، مع إظهار زر "تم" (Done) في قائمة الطلبات النشطة لإنهاء الطلب مباشرةً بعد تجهيزه. وإصلاح الفشل في اختبارات الأوامر نتيجة هذه الإضافة.
- النطاق: 
  - `public/views/pos-view.html` و `order-save.js` و `order-cart.js` لإضافة المربع.
  - `orders.service.js` لاستقبال ومعالجة المدفوعات التلقائية.
  - `order-active-list.js` و `order-payment.js` للزر "تم" وتعديل حسابات الضرائب.
  - ملفات اللغات `public/i18n/ar.js` و `en.js`.
- القرار: تم دعم التعامل مع حالة الدفع المسبق بتمرير `is_prepaid`، وتم التعديل على `orders.service.js` لضبط مبلغ الدفع تلقائياً ليساوي المجموع في حالة الدفع المسبق، وتم إصلاح خلل الدفع ليدعم الصفر ويقوم بالدفع التلقائي الكامل عند إنهاء الطلب دون تحديد مبلغ. 
- قاعدة البيانات: لا يوجد.
- الاختبارات: تم تشغيل `pnpm test` بنجاح (27/27) و `pnpm run check` واجتياز 120 ملف.
- المخاطر: تم إصلاح فشل `05-orders-atomic.cjs` بالكامل وتجاوز مشكلة تعديلات الدفع، المخاطر منخفضة الآن.
- نقطة البدء التالية: مراجعة الهيكلة أو رفع التحديثات إلى جيت هب.

## 2026-09-09 — تطبيق شكل الفاتورة الضريبية للنظام التايلاندي

- المنفذ: Antigravity.
- الهدف: تطبيق متطلبات الفاتورة الضريبية التايلاندية (إظهار الرقم الضريبي وحساب نسبة الـ 7% شاملاً أو غير شامل) في الطباعة ومعاينة الإعدادات، وإصلاح خطأ شهادة TLS المنتهية في الاختبارات.
- النطاق: `public/js/features/orders/order-receipt-printing.js`، `tests/19-tls.cjs`.
- القرار: 
  - تمت إضافة منطق الحساب المباشر للضريبة (مشمولة/غير مشمولة) داخل نموذج توليد HTML الفاتورة بناءً على `tax_type`، وتم إدراج ترويسة "Tax Invoice / ใบกำกับภาษีอย่างย่อ" تلقائياً عند ضبط الرقم الضريبي.
  - تم تعطيل التحقق من صلاحية شهادة TLS في اختبارات `tests/19-tls.cjs` عبر `rejectUnauthorized: false` لأن الشهادة المحلية انتهت.
- قاعدة البيانات: لا يوجد تعديل.
- الاختبارات: تم بنجاح اجتياز `pnpm test` (27/27) و `pnpm run check`.
- المخاطر: لا يوجد حالياً، الفاتورة يتم معاينتها بشكل فوري.
- نقطة البدء التالية: اكمال المهام المتبقية ان وجدت أو الدفع للـ Git.

## 2026-09-09 — إصلاح توجيه مستخدم المطبخ لشاشة KDS

- المنفذ: Antigravity.
- الهدف: حل مشكلة ظهور شاشة الكاشير لمستخدمي المطبخ عند تسجيل الدخول.
- النطاق: `public/js/core/authentication.js`.
- القرار: إضافة شرط للتحقق إذا كان دور المستخدم هو `kitchen` أو إذا كان يملك صلاحية `kitchen` فقط بدون صلاحية `pos`، فيتم توجيهه مباشرةً إلى صفحة `kds.html#session=...`.
- السلوك قبل/بعد: قبل: كان الدخول يفتح دائماً الواجهة الأساسية (POS). بعد: يتم التوجيه التلقائي للمطبخ في حال كان هذا هو دوره الأساسي.
- الاختبارات: جرى تشغيل `pnpm test` و `pnpm run check`.


## 2026-09-09 — تطبيق حد الخصم للموظفين وتحديث هيكلة الوثائق

- المنفذ: Antigravity.
- الهدف: تفعيل ميزة `max_discount` التي كانت موجودة شكلياً فقط لتجنب استغلال الصلاحيات، وتحديث الوثائق الرسمية للمشروع لتعكس التقسيم الموديولي الجديد.
- النطاق: `src/database/migrations.js`، `src/routes/employees.routes.js`، `src/middleware/auth.middleware.js`، `src/services/orders.service.js`، `public/js/app.js`، `public/js/employees.js`، وجميع ملفات التوثيق والترجمة.
- القرار: نقل التحقق من الخصم إلى Backend ضمن `orders.service.js` وعدم السماح حتى للمدير بتجاوز النسبة الخاصة به بدون صلاحية صريحة.
- السلوك قبل/بعد: قبل: كان يمكن لأي شخص تجاوز حد الخصم، وتمثيل `0%` كـ `100%`. بعد: الواجهة تخفي حقل الخصم لمن ليس له صلاحية، والسيرفر يرفض أي فاتورة تتجاوز `max_discount` للموظف. تم تحديث الوثائق لتوضيح شجرة المجلدات الحالية.
- قاعدة البيانات: أضيف عمود `max_discount` لجدول `employees` بقيمة افتراضية 100 عبر migration سابقة.
- الترجمة: أضيفت مفاتيح `emp_max_discount` و `emp_max_discount_hint` للغتين العربية والإنجليزية.
- الأمان والصلاحيات: تم تأمين مسارات إنشاء وتحديث الطلبات لفرض قيود الخصم بشكل قاطع.
- الاختبارات: `pnpm test` و `pnpm run check`.
- المخاطر أو الدين المتبقي: يحتاج المستخدمون الحاليون إلى مراجعة قيم الخصم الخاصة بهم للتأكد من عدم وجود أخطاء في الإدخال السابق.
- نقطة البدء التالية: التأكد من طباعة الفواتير بشكل صحيح مع الخصومات المطبقة.


## 2026-09-09 — إدارة وتقرير موظفي HR

- المنفذ: Codex.
- الهدف: إكمال دورة موظف شؤون الموظفين: تعديل، تعطيل آمن، وطباعة تقرير فردي أو جماعي.
- النطاق: `attendance.routes.js`، `attendance-admin.js`، لوحة الحضور، CSS، الترجمة، واختبار 27.
- القرار: الحذف الظاهري فقط (`active=0`) للحفاظ على سجلات الحضور والتدقيق؛ التقارير الفردية والجماعية تفتح قالب طباعة مستقلًا ولا تغيّر بيانات النظام.
- السلوك قبل/بعد: أصبحت كل صفوف الموظفين تحتوي تعديل وتعطيل وطباعة، وأضيف زر طباعة تقرير الجميع.
- قاعدة البيانات: لا جداول جديدة؛ أضيف مسار DELETE للتعطيل فقط.
- الترجمة: أضيفت مفاتيح التعديل والتعطيل والتقارير بالعربية والإنجليزية.
- الأمان والصلاحيات: التعديل والتعطيل محميان بـ`requireAdmin`، والطباعة تعتمد على البيانات المحملة من مسار مصادق.
- الاختبارات: `pnpm test` نجح 27/27، و`pnpm run check` نجح وفحص 120 ملفًا.
- المخاطر أو الدين المتبقي: تقرير الحضور الشهري المتقدم والتصدير Excel خاص بـHR مرحلة لاحقة.
- نقطة البدء التالية: إضافة فلترة شهرية وتقرير دوام تفصيلي بعد اعتماد شكل التقرير التجاري.

## 2026-09-09 — نافذة إضافة موظف HR المنسقة

- المنفذ: Codex.
- الهدف: استبدال نافذة `prompt` البسيطة بنموذج داخلي مرتب عند إضافة موظف شؤون الموظفين.
- النطاق: `attendance-admin.js`، `admin-and-components.css`، `ar.js`، `en.js`، `index.html`، `sw.js`.
- القرار: استخدام `openModal` الموحد للمشروع، مع نموذج ثنائي الأعمدة يستجيب للشاشات الصغيرة.
- السلوك قبل/بعد: أصبح الإدخال يشمل الاسم العربي والإنجليزي، الرمز الوظيفي، الهاتف، القسم، المسمى، نوع الدوام، وتاريخ التعيين، مع تحقق الاسم والرمز قبل الإرسال.
- قاعدة البيانات: لا تغيير؛ نفس مسار `/api/attendance/employees`.
- الترجمة: أضيفت مفاتيح الحقول والحالات بالعربية والإنجليزية.
- الأمان والصلاحيات: بقي الحفظ محميًا بصلاحية المدير في المسار الخلفي.
- الاختبارات: ستشمل الدفعة `pnpm test` و`pnpm run check` بعد التعديل.
- المخاطر أو الدين المتبقي: تعديل موظف موجود سيحتاج نافذة مماثلة في دفعة لاحقة.
- نقطة البدء التالية: إضافة نافذة تعديل موظف HR مع تعبئة البيانات الحالية.

## 2026-09-09 — تحسين أزرار وترجمة قسم الحضور

- المنفذ: Codex.
- الهدف: معالجة ملاحظات الواجهة الظاهرة في الصور على قسم شؤون الموظفين والحضور.
- النطاق: `attendance-admin.js`، `admin-and-components.css`، `i18n.js`، `index.html`، `sw.js`.
- القرار: أزرار القسم تستخدم هوية NOKTA المتدرجة مع حالات hover/ضغط، وتُعاد الترجمة بعد وصول البيانات الديناميكية.
- السلوك قبل/بعد: أزيل شكل أزرار المتصفح الافتراضي، وأصبح تبديل اللغة ينعكس على العناوين والحالات وقائمة أجهزة البصمة بعد التحميل.
- قاعدة البيانات: لا تغيير.
- الترجمة: لا مفاتيح جديدة؛ استُخدمت مفاتيح HR الموجودة بالعربية والإنجليزية.
- الأمان والصلاحيات: لا تغيير.
- الاختبارات: `pnpm test` نجح 27/27، و`pnpm run check` نجح وفحص 120 ملفًا.
- المخاطر أو الدين المتبقي: يلزم تحديث الصفحة مرة واحدة لتفعيل Service Worker الجديد v96.
- نقطة البدء التالية: مراجعة بقية أزرار الإدارة بصريًا وتوحيدها تدريجيًا دون تغيير عقود API.

## 2026-09-09 — إنشاء قسم شؤون الموظفين والحضور والبصمة

- المنفذ: Codex.
- الهدف: إضافة قسم مستقل داخل الإدارة للحضور والانصراف، منفصل عن موظفي الكاشير، مع صلاحية مستقلة وتجهيز ربط أجهزة البصمة.
- النطاق: جداول `hr_employees` و`attendance_records` و`biometric_devices`، المسار `src/routes/attendance.routes.js`، لوحة الإدارة ووحدة الواجهة، الترجمة، Service Worker، وصلاحيات الموظفين واختبار 27.
- القرار: عدم إعادة استخدام جدول `employees` للحضور. أجهزة البصمة تسجل المزود وبيانات الاتصال فقط؛ لا تحفظ صور أو قوالب البصمة داخل قاعدة POS.
- السلوك قبل/بعد: أضيف تبويب «شؤون الموظفين والحضور» مع إنشاء موظف HR وتسجيل الحضور وتسجيل الأجهزة. القراءة بصلاحية `attendance` والكتابة للمدير.
- قاعدة البيانات: ثلاث جداول وفهارس مع إنشاء متوافق عند الترحيل، دون تعديل الطلبات أو المبيعات.
- الترجمة: مفاتيح عربية وإنجليزية للقسم والحالات والأزرار.
- الأمان والصلاحيات: `requirePermission('attendance')` للقراءة و`requireAdmin` للكتابة.
- الاختبارات: `node tests/27-attendance.cjs` و`pnpm test` نجحا 27/27، و`pnpm run check` نجح وفحص 120 ملفًا.
- المخاطر أو الدين المتبقي: لا يوجد Adapter لجهاز محدد حتى اعتماد موديل وبروتوكوله. التسجيل اليدوي مؤقت وسيستبدل بنموذج كامل.
- نقطة البدء التالية: اعتماد موديل جهاز البصمة ثم بناء Adapter قراءة فقط واختبار المطابقة قبل المزامنة التلقائية.

## 2026-09-09 — اكتمال تقسيم ملفات الواجهة والنوافذ

- المنفذ: Codex.
- الهدف: إكمال تقسيم الملفات الكبيرة والنوافذ بأسماء واضحة وروابط ثابتة من دون تغيير السلوك التجاري.
- النطاق: نواة الواجهة، الطلبات، المنيو، الورديات، الإعدادات، تقارير المبيعات، CSS، الترجمة، قوالب HTML، لوحة الإدارة، Service Worker، واختبارات الربط.
- القرار: التقسيم حسب المسؤولية التجارية مع الإبقاء مؤقتًا على واجهات الدوال العامة لضمان التوافق. أصبح `index.html` قشرة تشغيل، وتحمل الشاشات من مسارات ثابتة مسجلة في `view-loader.js`. فصلت كل لوحة إدارية في ملف مستقل، وفصل قاموسا العربية والإنجليزية عن محرك الترجمة.
- السلوك قبل/بعد: لا تغيير مقصود في البيع أو الإغلاق أو الطباعة أو الصلاحيات. حذفت الملفات الجامعة القديمة بعد نقل محتواها كاملًا إلى `public/js/core` و`public/js/features`. أصبح `style.css` مجمّعًا صغيرًا لسبعة ملفات مرتبة مع الحفاظ على ترتيب cascade الأصلي.
- قاعدة البيانات: لا migration ولا تغيير schema ضمن التقسيم.
- الترجمة: القواميس موجودة الآن في `public/i18n/ar.js` و`public/i18n/en.js`، والمحرك في `public/i18n.js`، واختبار تطابق المفاتيح يقرأ الثلاثة بالترتيب نفسه المستخدم في المتصفح.
- الأمان والصلاحيات: لم تتغير مسارات API أو حراساتها. مسارات القوالب ثابتة في الكود وليست مدخلات مستخدم. بقيت كتابة الطلبات عبر `orders.service.js`.
- الاختبارات: نجح اختبار قوالب الواجهة ولوحات الإدارة. `pnpm test` نجح 26/26 بعد فصل الترجمة، و`pnpm run check` نجح وفحص 117 ملف JavaScript وجميع الأصول المحلية. `pnpm run android:sync` نجح.
- المخاطر أو الدين المتبقي: الدوال العامة المشتركة باقية كطبقة توافق؛ تحويلها إلى وحدات ذات واجهات خاصة يكون مرحلة مستقلة لاحقًا. ملفات CSS مقسمة حسب مناطق الواجهة مع الحفاظ على ترتيبها، ويمكن تحسين أسماء selectors تدريجيًا دون قص ميكانيكي جديد.
- نقطة البدء التالية: بناء وحدة الحضور المستقلة عن حسابات الكاشير وفق القسم 5 من `ARCHITECTURE-PLAN.md`، بدءًا من مخطط البيانات والصلاحيات والاختبارات، قبل أي ربط بجهاز بصمة فعلي.

## 2026-09-09 — النسخ الكاملة عند الإغلاق وتصدير Excel وتقسيم قائمة الطلبات

- المنفذ: Codex.
- الهدف: إنشاء نسخة نظام كاملة تلقائيًا عند الإغلاق الإداري، دعم نسخة يدوية كاملة، إضافة Excel للتقارير، ومواصلة تقسيم `orders.js`.
- النطاق: خدمة النسخ التلقائي، مسارات النسخ والإغلاق والتقارير، واجهات الإغلاق والإعدادات والتقارير، الترجمة، Service Worker، ExcelJS، واختبارات النسخ وExcel، و`public/js/features/orders/order-active-list.js`.
- القرار: استخدام نسخة SQLite فعلية للإغلاق والتنزيل اليدوي كي تشمل كل الجداول الحالية والمستقبلية. الإبقاء على JSON الحالي للاستعادة المنظمة. إنشاء XLSX حقيقي في الخادم بأوراق منفصلة بدل CSV. فصل قائمة الطلبات النشطة وحدها عن نافذة الدفع.
- السلوك قبل/بعد: الإغلاق الكلي من المدير يحفظ نسخة كاملة في `data/backups/daily-closing`. شاشة النسخ توفر تنزيل قاعدة كاملة يدويًا، إضافة إلى JSON السابق. شاشة التقارير توفر زر Excel. عرض الطلبات النشطة لم يتغير لكن تعريفه انتقل إلى ملف مستقل.
- قاعدة البيانات: لا migration ولا تغيير schema. أضيف اعتماد `exceljs` إلى الحزم وملف القفل.
- الترجمة: أضيفت مفاتيح عربية وإنجليزية لتنزيل قاعدة النظام، نجاح/فشل نسخة الإغلاق، وتصدير Excel.
- الأمان والصلاحيات: تنزيل قاعدة النظام محمي بـ`requireAdmin`. تقرير Excel محمي بصلاحية `reports`. نسخة SQLite تحتوي بيانات الدخول المجزأة ويجب حفظها في مكان آمن.
- الاختبارات: اختبار النسخ فتح ملف SQLite الناتج وقرأ بياناته. اختبار Excel فتح XLSX الناتج وتحقق من أربع أوراق وقيم رقمية وعربية. `pnpm test` نجح 25/25، و`pnpm run check` نجح وفحص 86 ملف JavaScript والأصول بعد التقسيم النهائي.
- المخاطر أو الدين المتبقي: اختيار مجلد خارجي ثابت يحتاج إعدادًا منفصلًا مع تحقق صلاحيات المسار؛ الحفظ المضمون حاليًا داخل مجلد بيانات النظام، والتنزيل اليدوي يسمح باختيار المكان حسب إعدادات المتصفح. استعادة SQLite المباشرة غير مفعلة؛ الاستعادة الحالية تستخدم JSON المتحقق منه.
- نقطة البدء التالية: فصل نافذة الدفع إلى `public/js/features/orders/order-payment.js` مع اختبار تعديل الأصناف والدفع، ثم فصل اختيار العميل إلى `order-customer-picker.js`.

## 2026-09-09 — إصلاح بوابة الاختبارات وأول تقسيم للطلبات

- المنفذ: Codex.
- الهدف: بدء التقسيم بأسماء واضحة مع إثبات الاندماج بعد كل نقل.
- النطاق: `src/app.js`، `public/js/orders.js`، `public/js/features/orders/order-production-printing.js`، `public/js/features/orders/order-receipt-printing.js`، `public/index.html`، `public/sw.js`، `tests/13-offline.cjs`، ووثائق الخطة والتسليم.
- القرار: فصل طباعة إيصال العميل عن طباعة تذاكر المطبخ لأن لكل منهما مسؤولية ومستهلكين مختلفين، مع إبقاء أسماء الدوال العامة مؤقتًا للتوافق مع بقية الواجهة.
- السلوك قبل/بعد: لا تغيير مقصود في الطباعة أو البيع؛ تغير مكان تعريف الدوال وترتيب تحميل الملفات فقط. أصبح مشغل الاختبارات يخرج بصورة صحيحة بدل الفشل الزائف بعد 30 ثانية.
- قاعدة البيانات: لا يوجد.
- الترجمة: لا يوجد نص ظاهر جديد.
- الأمان والصلاحيات: لم تتغير المسارات أو الصلاحيات أو معالجة بيانات الإيصال الآمنة.
- الاختبارات: `node tests/01-sql.cjs` نجح. `pnpm test` نجح 24/24 بعد إصلاح المؤقت، ونجح 24/24 بعد كل من نقل طباعة الإنتاج ونقل إيصال العميل. `pnpm run check` نجح بعد النقل النهائي وفحص 83 ملف JavaScript والأصول.
- المخاطر أو الدين المتبقي: الدوال ما زالت عالمية للحفاظ على التوافق؛ يعالج ذلك في مرحلة عقود الواجهة. الملفات `daily-closing.js` و`menu.js` و`app.js` تحتوي تغييرات محلية سابقة، فلا يبدأ نقلها قبل مراجعة الفروق وتحديد ملكيتها.
- نقطة البدء التالية: فصل قائمة الطلبات النشطة إلى `public/js/features/orders/order-active-list.js` بعد حصر اعتمادها على نافذة الدفع، ثم اختبار 24/24 والفحص العام.

## قالب الإدخال

```text
## YYYY-MM-DD — عنوان واضح

- المنفذ: اسم المبرمج أو المساعد.
- الهدف: لماذا طُلب التغيير.
- النطاق: الملفات أو الوحدات التي تغيرت.
- القرار: أهم قرار معماري ولماذا.
- السلوك قبل/بعد: وصف مختصر قابل للتحقق.
- قاعدة البيانات: migration أو "لا يوجد".
- الترجمة: مفاتيح ar/en أو "لا يوجد نص ظاهر".
- الأمان والصلاحيات: ما تمت مراجعته.
- الاختبارات: الأوامر والنتائج الفعلية.
- المخاطر أو الدين المتبقي: ما لم يُحل.
- نقطة البدء التالية: أول خطوة عملية للمطور التالي.
```

## 2026-09-09 — دراسة وخطة إعادة الهيكلة

- المنفذ: Codex.
- الهدف: إعداد تقسيم دقيق للكود والنوافذ، وتسهيل الإضافات المستقبلية ومنها الحضور وأجهزة البصمة.
- النطاق: إضافة `ARCHITECTURE-PLAN.md` و`audit/CHANGELOG.md`، وربط بروتوكول التغيير بـ`AGENTS.md`.
- القرار: اعتماد ترحيل تدريجي حسب الميزة مع طبقات توافق، وعدم تقسيم الملفات الكبيرة بالقص المباشر بسبب الحالة العامة المشتركة.
- السلوك قبل/بعد: لا يوجد تغيير في سلوك التطبيق أو قاعدة البيانات.
- قاعدة البيانات: لا يوجد.
- الترجمة: لا يوجد نص ظاهر داخل التطبيق.
- الأمان والصلاحيات: نصت الخطة على فصل صلاحيات الحضور وعدم تخزين صور أو قوالب البصمات داخل POS.
- الاختبارات: `pnpm run check` نجح وفحص 81 ملف JavaScript والأصول. أوقف `pnpm test` بعد ظهور أن مشغل المجموعة يسجل اختبارات طبعت PASS كـFAIL عند مهلة 30 ثانية؛ يلزم إصلاح هذا في المرحلة 0.
- المخاطر أو الدين المتبقي: توجد تغييرات محلية سابقة غير محفوظة في ملفات حساسة؛ لم تُعدّل أو تُلغَ. ملف `audit/verification.json` كان متغيرًا قبل الدراسة وقد يحدّثه مشغل الاختبارات.
- نقطة البدء التالية: تنفيذ المرحلة 0 من `ARCHITECTURE-PLAN.md`، بدءًا من عزل سبب عدم خروج عمليات الاختبار.
