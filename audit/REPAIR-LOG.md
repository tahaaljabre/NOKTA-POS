# سجل الإصلاح والتحقق / Repair log

تاريخ البدء: 2026-09-07. طلب المستخدم: إصلاح المشاكل بالتتابع، تشغيل التطبيق وتجربة كل إصلاح قبل التالي، وتوثيق السلوك مع العربية والإنجليزية.

الاختبارات تعمل بخادم HTTP حقيقي وقاعدة SQLite في الذاكرة. بيانات المنشأة لا تستخدم للاختبار. النسخة السابقة من المصدر محفوظة في baseline/source-before-fixes.zip. تقرير PROJECT-AUDIT.ar.md وresults.json يوثقان حالة ما قبل الإصلاح، وليسا نتيجة الاختبارات الجديدة.

## 01 — اقتباس قيم SQL

استبدال الاقتباس المزدوج لقيم now/occupied/empty باقتباس SQL المفرد في المسارات. التحقق: `node tests/01-sql.cjs`؛ يشغّل التطبيق ويجرب طاولة وطلبًا وإتمام الدفع والمخزون وفتح/إغلاق يوم ومزامنة.

نتيجة 01: PASS.

## 02 — معاملات قاعدة البيانات المسماة
تم تمرير الكائن إلى run/get/all مباشرة والمصفوفة بالنشر. تشغيل التطبيق وتصدير نسخة واستعادتها نجح في tests/02-bindings.cjs. نتيجة: PASS.

## 03 — استعادة متكاملة
النسخة v2 تشمل كل الجداول، وتتحقق قبل الحذف وتستعيد ضمن معاملة واحدة. النسخة الكاملة القديمة ترفض برسالة ترحيل؛ النسخة الكاملة تحتوي بيانات اعتماد مجزأة وتخص المدير فقط. tests/03-backup.cjs: PASS حتى مع فشل متعمد داخل الاستعادة.

## 04 — تأكيد المزامنة قبل حذف الطابور
401/500 لا تمسح الطلبات. تحذف المعرفات المؤكدة فقط وتنتظر اكتمال IndexedDB. اختبار التطبيق ورفض/نجاح جزئي: tests/04-sync-queue.cjs PASS.

## 05 — حفظ الطلب ومعاملته
خدمة orders.service تجمع الرأس والعناصر والعداد والطاولة والتدقيق والآثار. tests/05-orders-atomic.cjs PASS: فشل وسط الحفظ يرجع كل شيء، المعرف الثابت يمنع التكرار، تعارض الطاولة وتعديل نسخة قديمة يرفضان.

## 06 — عرض البيانات كنص
إضافة safety.js وتهريب مواضع HTML للأسماء والملاحظات والتدقيق والصور، وإزالة أسماء المستخدمين من نصوص أحداث JavaScript. tests/06-rendering.cjs PASS. الدخول والمنيو جُربا في المتصفح.

## 07 — حساب موحد
المباشر والمزامنة يستخدمان نفس خدمة البيع. snapshots الأسعار القديمة محفوظة عند تعديل الملاحظة؛ كميات القطع صحيحة فقط، الخصومات ضمن 0–100، والإضافات مرتبطة بالصنف. tests/07-pricing.cjs PASS. الطلبات المرفوضة ترجع ضمن failedOrders.

## 08 — المخزون والنقاط
كل آثار البيع ضمن نفس المعاملة. الإلغاء والحذف يعكسان المخزون والنقاط مرة واحدة؛ إعادة الإرسال لا تكرر الأثر. tests/08-stock.cjs PASS. لا تُعاد كتابة أرصدة المنشأة التاريخية تلقائيًا؛ يلزم تسوية إذا تأثرت بالنسخة السابقة.

## 09 — المخزون والإضافات
استعمال api المصادق بدل fetch وgetAuthHeaders غير الموجود. أضيف GET inventory/:id/logs. تعديل المخزون صار معاملة مع سجل تدقيق ومنع السالب. tests/09-inventory.cjs PASS.

## 10 — تعديل الفاتورة
إزالة DELETE غير الموجود؛ إرسال العناصر النهائية دفعة واحدة مع version. شاشة الدفع تطبع الاستجابة المحسوبة من الخادم. tests/10-invoice-edit.cjs PASS.

## 11 — الحسابات والتهيئة
منع فقد آخر مدير. setup_complete يقفل التهيئة بعد أول استخدام. منع PIN مكرر وtoken_rev يبطل الجلسات عند التعديل. مسار setup مشمول بحد المحاولات. أزيل الدخول المحلي غير المصادق. tests/11-auth.cjs PASS.

## 12 — الصلاحيات
تاريخ الفواتير والمحذوفات محمي من جميع مسارات القراءة؛ POS يرى النشط فقط، مع استثناء كشف يومه المفلتر باسمه. خصومات وإلغاء بصلاحيات صريحة، والنسخ للمدير. tests/12-permissions.cjs PASS.

## 13 — الحفظ دون اتصال
مسار الحفظ موصول بطابور دائم بمعرف عشوائي ثابت وهوية الموظف. إعادة المزامنة عند الدخول ورجوع الاتصال وكل 15 ثانية للحساب الحالي فقط. أخطاء 4xx لا تخزن كطلبات أوفلاين. tests/13-offline.cjs PASS.

## 14 — Service Worker والتخزين
توحيد IndexedDB v2، إزالة localStorage من worker، منع كاش API المشترك بين الحسابات، حفظ كل ملفات التطبيق وتحديثها network-first. مفتاح settings للكاش صار صحيحًا، والكاش يحذف الأصناف القديمة. tests/14-worker.cjs PASS. الأوفلاين يتطلب جلسة دخول سابقة؛ لا يوجد تجاوز للدخول عند انقطاع الخادم.

## 15 — يوم العمل
منطقة المنشأة Asia/Bangkok افتراضيًا حسب مواصفات المطعم، وقابلة للتغيير. التقارير والإغلاق تستعمل وقت الإكمال وحدود UTC لليوم المحلي. tests/15-business-day.cjs PASS. لا تغيّر تاريخ المبيعات القديمة.

## 16 — المطبخ
GET orders/kitchen يقدم بيانات التجهيز فقط، وPOST orders/:id/ready يحدث attributes دون تسعير. صلاحية kitchen مستقلة وversion يمنع الكتابة القديمة. tests/16-kitchen.cjs PASS.

## 17 — Socket والجلسات
التحقق من المستخدم النشط وtoken_rev قبل الاتصال وكل بث، مع قطع الجلسة المبطلة. tests/17-socket.cjs PASS لاختبار middleware والبث.

## 18 — تشغيل الخادم والإنتاج
إضافة مجلد بيانات قابل للتحديد، سر جلسة دائم، رؤوس no-store لواجهات API، 404 بصيغة JSON، إخفاء تفاصيل 500، ورسائل خطأ عربية/إنجليزية. وضع الإنتاج لا يبدأ بلا شهادة TLS. tests/18-server.cjs وtests/19-tls.cjs: PASS. شهادة الاختبار ليست شهادة نشر.

## 19 — الترجمة والتحقق من المدخلات
أضيفت مفاتيح الصلاحيات والمطبخ والمنطقة الزمنية وتحذير النسخة الاحتياطية بالعربية والإنجليزية. اكتمل التحقق من المخزون والإضافات والطاولات والفواتير وحدود الصفحات. tests/20-translations.cjs وtests/21-validation.cjs: PASS.

## 20 — قاعدة البيانات والبناء
استبدال الاعتماد الأصلي الذي يحتاج أدوات C++ بواجهة `node:sqlite` المدمجة، مع اشتراط Node.js 22.5+. أصبح `pnpm-lock.yaml` ملف القفل الوحيد. نجحت مزامنة Capacitor Android، واختبارات التطبيق 21/21 بعد النقل.

## 21 — مراجعة العرض النهائية
تهريب بيانات المنشأة والطابعات وأسماء الأصناف والإضافات في جداول الإدارة وإيصالات الطباعة. ملفات `AGENTS.md` و`PROJECT-HANDOFF.md` تسجل الثوابت وأوامر الفحص والمخاطر التشغيلية لأي مطور أو ذكاء اصطناعي لاحق.

## 22 — الاعتمادات والعمل المحلي
فحص pnpm كشف ثغرتين متوسطتين في qs؛ ثُبتت 6.16.0 عبر override وأصبح فحص الإنتاج صفر ثغرات معروفة. نُقلت Chart.js إلى `public/vendor` وحذف اعتماد الخط والصوت على الإنترنت؛ صوت المطبخ يولد محليًا عبر Web Audio. صُححت تسمية طلب المطبخ الذي لا يملك طاولة، وتحديث Service Worker صار يتفعّل فورًا.

## 23 — انتقال جلسة شاشة المطبخ
بعض نوافذ Android والمتصفحات تفصل localStorage عند فتح شاشة جديدة، فكانت شاشة المطبخ تفقد جلسة الدخول. رابط المطبخ يمرر Token الموقع داخل URL fragment (لا يرسل إلى الخادم)، وتلتقطه الشاشة ثم تحذفه من شريط العنوان فورًا. الفتح المباشر دون جلسة يبقى محميًا.

## 24 — إزالة نسخة الدخول القديمة من المتصفح
الخادم يرسل no-store أيضًا للصفحة الرئيسية وService Worker. التسجيل يستخدم updateViaCache:none، ويعيد تحميل الصفحة مرة واحدة تلقائيًا عند استلام العامل الجديد. هذا يستبدل النسخة القديمة التي كانت تسمح بدخول محلي عند توقف الخادم وتعيد index.html لمسار المطبخ. تحقق مباشر: PIN عشوائي أعاد 401، و`/kds.html` احتوى شاشة KDS ولم يحتو لوحة PIN.

## 25 — وصول المدير عبر Tailscale
ثُبت Tailscale 1.102.3 وربط الكمبيوتر بالشبكة الخاصة، وفُعّل Serve بخلفية دائمة من HTTPS إلى `127.0.0.1:3000`. الرابط موثق في `REMOTE-ACCESS.md` واختُبر في متصفح مستقل ففتح شاشة NOKTA POS. لا يوجد فتح لمنفذ عام في الراوتر.
سُجلت مهمة Windows باسم `NOKTA POS Server` لتشغيل `node server.js` مباشرة عند دخول المستخدم. اختُبرت بعد إيقاف الخادم اليدوي: حالة المهمة Running، المنفذ 3000 مستمع، وواجهة الفحص أعادت HTTP 200، ثم نجح الرابط البعيد مرة أخرى.

## 26 — إصدار أندرويد وإعداد الخادم
أضيفت شاشة ربط عربية/إنجليزية تقبل عنوان الشبكة المحلية أو رابط Tailscale، وتتحقق من صحة الرابط وتحفظه، مع إمكانية الرجوع لتغييره. عُطّل Android Backup لمنع نسخ بيانات WebView الحساسة خارج التطبيق. اكتُشف أن مشغّل Capacitor السابق كان ينتهي بلا تنفيذ؛ أُصلح واختُبر أن الشاشة الجديدة موجودة داخل APK نفسه. نجح `assembleRelease` وفحص Android Lint. النسخة الحالية غير موقعة إلى أن يُنشأ مفتاح الإصدار المملوك لصاحب البرنامج.

## 27 — نسخة ويندوز
إذا وجد Electron خادم NOKTA يعمل على المنفذ فإنه يستخدمه بدل تشغيل نسخة ثانية متعارضة. وإذا بدأ الخادم بنفسه يغلق فقط الخادم الذي يملكه. تحفظ النسخة المغلفة البيانات تحت مجلد مستخدم Windows بدل مجلد التثبيت. بُني مُثبّت NSIS وشُغلت النسخة المغلفة فعليًا؛ ظهرت نافذة NOKTA واتصلت بالخادم HTTP 200. أُصلح مرجع الأيقونة المفقود وأعيد البناء. ملف Windows غير موقّع رقميًا لعدم توفر شهادة Code Signing تجارية.

## 28 — النسخ الاحتياطي التلقائي
الخادم ينشئ قاعدة SQLite احتياطية متسقة عند التشغيل وكل 24 ساعة في `data/backups` ويحذف النسخ الأقدم من 30 يومًا. الاختبار أنشأ نسخة وفتحها وقرأ البيانات منها، ثم اختبر سياسة الحذف. بعد إعادة تشغيل خدمة المطعم أُنشئت نسخة فعلية بحجم 1,880,064 بايت. هذه نسخة محلية؛ يلزم نسخ خارجي دوري للحماية من تعطل أو سرقة كمبيوتر المطعم.

## 29 — نتيجة التحقق الحالية
فحص المصدر: 78 ملف JavaScript ومراجع الأصول PASS. اختبارات التطبيق: 24/24 PASS. Android release build وLint: PASS. Windows NSIS build: PASS. تشغيل Windows المغلف: PASS. الوصول المحلي وإنشاء النسخة التلقائية: HTTP 200 وPASS.
