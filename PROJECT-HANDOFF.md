# Project Handoff: NOKTA POS

آخر تحديث: 2026-09-07

هذا الملف هو نقطة البداية لأي مطور أو ذكاء اصطناعي سيكمل العمل على المشروع. اقرأه مع `PROJECT-SPEC.md` قبل تعديل الكود.

## 1. تعريف المشروع

نظام نقاط بيع لمطعم يعمل داخل شبكة LAN، ويمكن تشغيله كتطبيق PWA على أجهزة الكاشير والنوادل. الواجهة عربية/إنجليزية، والعملة والإعدادات قابلة للتغيير.

التقنيات الحالية:

- Node.js + Express.
- SQLite على القرص بواسطة `node:sqlite` المدمج في Node.js، مع WAL ومعاملات ذرية.
- Vanilla JavaScript في الواجهة.
- Socket.IO للتحديث الفوري بين الأجهزة.
- IndexedDB للعمل دون اتصال ومزامنة الطلبات لاحقًا.
- Service Worker لتخزين الواجهة والعمل كـ PWA.

## 2. التشغيل

```powershell
$env:CI='true'; pnpm install --frozen-lockfile
pnpm start
```

الخادم يستمع افتراضيًا على:

```text
http://0.0.0.0:3000
```

للوصول من جهاز داخل نفس الشبكة استخدم عنوان الكمبيوتر المحلي، مثل:

```text
http://192.168.8.193:3000/
```

إعدادات المنفذ والعنوان وقاعدة البيانات موجودة في `src/config/app.config.js`.

مهم:

- يجب أن يبقى الكمبيوتر والخادم يعملان.
- إذا تغير عنوان IP يجب إرسال العنوان الجديد للأجهزة.
- سر الجلسات يولد مرة واحدة في `.session-key` داخل مجلد البيانات إن لم يحدد `JWT_SECRET`، ويبقى صالحًا بعد إعادة التشغيل.
- الإنتاج يتطلب `POS_TLS_KEY` و`POS_TLS_CERT` لشهادة HTTPS موثوقة.
- لم يتم إنشاء Git repository في المجلد الحالي، لذلك استخدم نسخًا احتياطية قبل التغييرات الكبيرة.

## 3. خريطة الملفات

### Backend

- `server.js`: نقطة تشغيل الخادم وتهيئة قاعدة البيانات وSocket.IO.
- `src/app.js`: Express middleware وتركيب كل API routes.
- `src/config/app.config.js`: إعدادات الخادم والمسارات.
- `src/database/schema.js`: مخطط SQLite الأساسي.
- `src/database/migrations.js`: إضافة الأعمدة المطلوبة لقواعد البيانات القديمة.
- `src/database/db.js`: تهيئة node:sqlite، التجزئة، والمعاملات وواجهات قاعدة البيانات.
- `src/middleware/auth.middleware.js`: إصدار والتحقق من Tokens والصلاحيات.
- `src/middleware/audit.middleware.js`: تسجيل عمليات التدقيق.
- `src/routes/*.routes.js`: مسارات الإدارة والطلبات والتقارير وغيرها.

### Frontend

- `public/index.html`: الهيكل الكامل للتطبيق والشاشات والنماذج.
- `public/js/app.js`: الحالة العامة، API، الدخول، التنقل، Socket، اللغة.
- `public/js/menu.js`: الأقسام، الأصناف، شاشة المنيو، وإدارة المنيو.
- `public/js/employees.js`: إدارة الموظفين والصلاحيات.
- `public/js/orders.js`: الطلبات والفواتير.
- `public/js/settings.js`: الإعدادات والنسخ الاحتياطي.
- `public/js/audit.js`: سجل التدقيق.
- `public/js/reports.js`: التقارير.
- `public/i18n.js`: الترجمة العربية والإنجليزية.
- `public/style.css`: التنسيق العام.
- `public/sw.js`: Service Worker والكاش والمزامنة.

## 4. ما تم إنجازه وإصلاحه

### إصلاح إدارة الأقسام والمنيو

- إصلاح عناوين جدول الأقسام التي كانت تظهر حرفيًا بصيغة `${currentLang...}` بسبب وجود JavaScript داخل HTML ثابت.
- إصلاح زر `+ قسم جديد`: كان يمرر `MouseEvent` إلى `openCategoryModal`، فكان يظهر خطأ `بيانات القسم غير صالحة`. الدالة الآن تتجاهل الحدث وتفتح نموذج الإضافة.
- دعم النسخ القديمة التي تستدعي `openCategoryModal(categoryId)` بالإضافة إلى النسخة التي تمرر كائن القسم.
- توحيد أزرار تعديل الأقسام على `openCategoryModalById`.
- إعادة تحميل بيانات الأقسام عند الحاجة.
- إضافة cache-busting لطلبات الأقسام الإدارية كي لا تعرض الأجهزة نسخة قديمة.
- رفع إصدارات ملفات الواجهة عدة مرات لتجاوز Service Worker القديم. الإصدارات الحالية في `index.html` هي `menu.js?v=22` و`app.js?v=23` و`employees.js?v=18` و`i18n.js?v=20`، وإصدار الكاش الحالي `pos-v43`.

### إصلاح الأسعار

- كان يمكن حفظ قيمة عشرية غير مقصودة مثل `39.98` بدل `40`.
- تم إضافة تحقق في `src/routes/items.routes.js` لمنع الأسعار السالبة أو غير الرقمية.
- يتم تقريب السعر والسعر الثاني إلى منزلتين عشريتين في الخادم.
- الواجهة تقرب السعر قبل الإرسال.
- تم تصحيح الصنف الذي كان محفوظًا بسعر `39.98` إلى `40`، والتحقق من عدم بقاء قيمته القديمة.

### إصلاح سلامة الطلبات والمزامنة

- تعديل الطلبات لا يعتمد على `total` المرسل من العميل.
- أسعار عناصر الطلب يعاد أخذها من قاعدة بيانات الأصناف بدل الثقة بسعر العميل.
- هوية الموظف في التدقيق تؤخذ من Token المصادق عليه بدل قيم body أو headers غير الموثوقة.
- المزامنة تعيد حساب إجمالي الطلب من العناصر والأسعار الموجودة في الخادم.
- المزامنة تستخدم الموظف المصادق عليه بدل `employee_id` القادم من الجهاز.

### حسابات الموظفين

تمت إضافة دعم:

- `username` لاسم المستخدم.
- `password_hash` لكلمة مرور الدخول، منفصل عن PIN.
- تسجيل الدخول باسم المستخدم وكلمة المرور يستخدم `password_hash`، مع fallback قديم إلى `pin` للحسابات القديمة.
- تعديل اسم المستخدم وكلمة المرور من نموذج الموظف.
- كلمة المرور الجديدة لا تظهر في قائمة الموظفين ولا ترجع من API.
- كلمة المرور تخزن باستخدام hashing الموجود في `src/database/db.js`.

Migration العمود موجود في `src/database/migrations.js`:

```text
employees.password_hash
```

### زر إظهار كلمة مرور المدير

- تمت إضافة زر العين في شاشة دخول المدير.
- الزر يبدل بين الإظهار والإخفاء.
- تمت إضافة `show_password` و`hide_password` للغتين.
- تمت إضافة دعم ترجمة `data-i18n-title`.

## 5. الصلاحيات والإدارة

- المدير `role=admin` يتجاوز الصلاحيات.
- الصلاحيات مخزنة في `employees.permissions` بصيغة JSON.
- مسارات الإدارة محمية من الخادم بواسطة `requirePermission` أو `requireAdmin`.
- إخفاء التبويبات في الواجهة ليس بديلًا عن حماية الخادم.
- إدارة الموظفين تتطلب صلاحية المدير.
- النسخ الاحتياطي والاستعادة تتطلب صلاحية الإعدادات.
- تعديل وحذف الطلبات لهما صلاحيات منفصلة.

الصلاحيات الشائعة:

```text
pos, menu, reports, settings, audit, employees, customers,
inventory, tables, daily_closing, edit_orders, delete_orders,
view_invoices, dashboard
```

## 6. الاختبارات التي تم تنفيذها

تم تنفيذ وفحص التالي:

- فحص صياغة JavaScript لملفات المشروع، ونجحت الملفات المعدلة.
- فحص API بدون تسجيل دخول، ويعيد `401` كما يجب.
- الدخول الفعلي من شبكة LAN بحساب اختبار إداري.
- إضافة قسم تجريبي وظهوره في جدول الإدارة، ثم حذفه.
- إضافة صنف تجريبي إلى قسم قديم وظهوره في شاشة الطلبات، ثم تنظيفه من قاعدة البيانات.
- فحص وجود عمود `password_hash` في قاعدة البيانات.
- ظهور حقول اسم المستخدم وكلمة المرور في نموذج تعديل الموظف.
- ظهور زر العين في شاشة دخول المدير.
- اختبار الصفحة وAPI على المنفذ `3000` وإرجاع `HTTP 200`.

لا تحفظ أرقام PIN أو كلمات المرور داخل هذا الملف. استخدم حسابًا تجريبيًا محدود الصلاحيات عند مشاركة التطبيق مع عميل.

## 7. الوصول الخارجي المؤقت

للوصول من خارج شبكة المطعم تم تثبيت `cloudflared` وإنشاء Quick Tunnel مؤقت. رابط Quick Tunnel يتغير عند إعادة تشغيله، ولا يجب اعتباره رابط إنتاج دائم.

أمر التشغيل:

```powershell
& 'C:\Program Files (x86)\cloudflared\cloudflared.exe' tunnel --url http://localhost:3000
```

الرابط الناتج من Cloudflare يجب نسخه من مخرجات الأمر في كل مرة. لا تضع رابط Quick Tunnel قديمًا في التوثيق أو في كود التطبيق.

ملاحظات أمنية:

- لا ترسل حساب المدير للعميل.
- أنشئ موظفًا تجريبيًا بصلاحيات محدودة.
- أوقف النفق بعد انتهاء التجربة.
- لا تفتح المنفذ `3000` مباشرة على الإنترنت.
- خذ Backup قبل مشاركة النظام.

## 8. نقاط يجب الانتباه لها مستقبلًا

- قاعدة البيانات in-memory ويتم حفظها دوريًا؛ يجب اختبار الإيقاف المفاجئ والنسخ الاحتياطي.
- لا تغيّر `schema.js` دون إضافة migration متوافقة في `migrations.js`.
- عند تعديل ملفات frontend، حدّث query version في `index.html` أو ارفع `CACHE_NAME` في `sw.js`.
- لا تعتمد على Cache API عند التحقق من بيانات إدارية جديدة.
- لا تثق في الأسعار أو هوية الموظف أو الإجمالي القادم من المتصفح.
- يجب إضافة اختبارات آلية لمسار إنشاء القسم، إنشاء الصنف، تسجيل الدخول باسم المستخدم، والأسعار.
- يفضل توحيد escaping في كل `innerHTML` لتقليل مخاطر XSS، خصوصًا أسماء الموظفين والأصناف والتفاصيل.
- يفضل إضافة transaction أقوى لتوليد أرقام الفواتير عند تعدد الأجهزة.
- `PROJECT-SPEC.md` يحتوي على وصف تاريخي وقد لا يطابق كل تفاصيل التنفيذ الحالي؛ الكود داخل `src` هو المرجع التشغيلي.

## 9. ترتيب العمل المقترح للمطور التالي

1. اقرأ هذا الملف و`PROJECT-SPEC.md`.
2. شغّل الخادم وتحقق من `http://localhost:3000/`.
3. افحص حالة قاعدة البيانات ووجود migrations.
4. لا تعدّل بيانات الإنتاج قبل أخذ نسخة من `data/pos.sqlite`.
5. اختبر مسار الإدارة بحساب محدود قبل استخدام حساب المدير.
6. بعد أي تعديل frontend، اختبر الكاش من نافذة خاصة أو ارفع إصدار Service Worker.
7. بعد أي تعديل backend، نفذ `node --check` للمسارات ثم اختبر API فعليًا.

## 10. حالة التشغيل عند كتابة الوثيقة

- المشروع موجود في: `C:\Users\User\OneDrive\Desktop\test\pos-web`
- المنفذ الافتراضي: `3000`
- الخادم مضبوط على `0.0.0.0` للوصول من LAN.
- لغة الواجهة الافتراضية: العربية.
- Cloudflare Tunnel أداة مثبتة، لكن رابط Quick Tunnel مؤقت ويتطلب تشغيل الأمر يدويًا عند الحاجة.

## 11. التحديثات الأخيرة (سبتمبر 2026)

- **ترقية قاعدة البيانات:** تم استبدال مكتبة `sql.js` (التي تعمل في الذاكرة) بمكتبة `better-sqlite3` لكتابة البيانات مباشرة على القرص وتفعيل وضع `WAL`، مما يمنع فقدان البيانات تماماً عند انقطاع الكهرباء.
- **إصلاحات المنطق والحسابات:** 
  - السيرفر أصبح يقرأ ويحسب أسعار الإضافات (Modifiers) ويدمجها مع السعر الأساسي للوجبة.
  - إصلاح خلل خصم المخزون ونقاط العميل عند قيام الكاشير بتعديل الفواتير المكتملة مسبقاً (Completed Orders).
- **تطبيق سطح المكتب (Desktop App):** تمت إضافة `Electron.js` وتجهيز ملف `electron.js` لتشغيل المشروع كتطبيق سطح مكتب منفصل (Windows/Mac) يحمل بداخله السيرفر لتشغيل الشبكة المحلية تلقائياً.
- أمر التشغيل الجديد للتطبيق: `npm run desktop`
